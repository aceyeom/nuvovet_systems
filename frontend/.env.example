# NuvoVet Frontend Environment Variables
# Copy to .env.local and adjust for your setup.

# Backend API URL — FastAPI server started with `cd backend && python3 -m uvicorn main:app --port 8000`
VITE_API_URL=http://localhost:8000
